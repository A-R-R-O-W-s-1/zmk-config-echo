name: 'Build ZMK Firmware'

on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: zmkfirmware/zmk-build-arm:3.5
      
    strategy:
      fail-fast: false
      matrix:
        include:
          - board: nice_nano_v2
            shield: echo_left
          - board: nice_nano_v2
            shield: echo_right

    steps:
      - name: Prepare workspace directory
        # Create a new, clean directory to act as our build environment
        run: mkdir zmk-workspace

      - name: Checkout user config into workspace
        # Checkout your config repo into a subdirectory named 'config'
        uses: actions/checkout@v4
        with:
          path: zmk-workspace/config

      - name: Initialize west workspace
        # Initialize the build environment from within the clean workspace directory
        working-directory: zmk-workspace
        run: west init -l config

      - name: Update west dependencies
        # Run west update from the initialized workspace. It will now find everything.
        working-directory: zmk-workspace
        run: west update

      - name: Export Zephyr CMAKE package
        working-directory: zmk-workspace
        run: west zephyr-export

      - name: Build firmware
        # Run the build from the workspace directory
        working-directory: zmk-workspace
        run: |
          west build -s zmk/app \
          -d "build/${{ matrix.shield }}_${{ matrix.board }}" \
          -b "${{ matrix.board }}" -- \
          -DSHIELD="${{ matrix.shield }}" \
          -DZMK_CONFIG="${{ github.workspace }}/zmk-workspace/config" \
          -Dsnippets="studio-rpc-usb-uart"

      - name: Archive firmware
        uses: actions/upload-artifact@v4
        with:
          name: firmware-${{ matrix.shield }}-${{ matrix.board }}
          path: |
            zmk-workspace/build/${{ matrix.shield }}_${{ matrix.board }}/zephyr/zmk.uf2